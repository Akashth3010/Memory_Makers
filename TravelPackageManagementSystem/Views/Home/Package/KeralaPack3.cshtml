@{
    ViewBag.Title = "Kerala Travel Package 3";
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@ViewBag.Title</title>
    <link rel="stylesheet" href="/css/PackTD.css" />
</head>
<body>

    <section class="hero">
        <img class="hero-img" src="/lib/Image/k31.jpg" alt="Kovalam Beach" />
        <div class="hero-content">
            <h1>Kerala: Kovalam & Varkala</h1>
            <p>3 Days • Beaches • Cliff Views • Ayurveda</p>
        </div>
    </section>

    <div class="container">
        <div class="flex">

            <div class="left-col">
                <div class="package-header">
                    <div class="package-title">
                        <h2 id="packageName">Discover Kerala: Kovalam Beach & Varkala Cliff</h2>
                        <div class="rating">★ 4.7 (784 reviews)</div>
                    </div>

                    <div style="text-align: right;">
                        <div style="font-size: 14px; color:#666;">Duration</div>
                        <div style="font-weight:700; font-size:16px;">3 Days / 2 Nights</div>
                    </div>
                </div>

                <div class="gallery">
                    <div id="mainImage" class="main-image" style="background-image: url('/lib/Image/k32.jpg');"></div>
                    <div class="thumbs">
                        <button class="thumb active" type="button" data-src="/lib/Image/k32.jpg" style="background-image: url('/lib/Image/k32.jpg');"></button>
                        <button class="thumb" type="button" data-src="/lib/Image/k33.jpg" style="background-image: url('/lib/Image/k33.jpg');"></button>
                        <button class="thumb" type="button" data-src="/lib/Image/k34.jpg" style="background-image: url('/lib/Image/k34.jpg');"></button>
                        <button class="thumb" type="button" data-src="/lib/Image/k35.jpg" style="background-image: url('/lib/Image/k35.jpg');"></button>
                    </div>
                </div>

                <div style="margin-top:16px;">
                    <h3>Overview</h3>
                    <p>Relax on Kerala’s golden coast. Enjoy Kovalam’s famous beaches, rejuvenate with Ayurveda therapies, and explore Varkala’s dramatic cliffs overlooking the Arabian Sea.</p>
                </div>

                <div class="itinerary">
                    <h3>Day-by-Day Itinerary</h3>
                    <div class="day">
                        <div class="day-header">
                            <h4>Day 1 — Arrival & Kovalam Beach</h4>
                            <div class="toggle">+</div>
                        </div>
                        <div class="day-body">
                            <p>Arrive in Kovalam. Visit Lighthouse Beach and Hawa Beach. Evening leisure with optional Ayurveda massage.</p>
                        </div>
                    </div>

                    <div class="day">
                        <div class="day-header">
                            <h4>Day 2 — Varkala Cliff & Sunset</h4>
                            <div class="toggle">+</div>
                        </div>
                        <div class="day-body">
                            <p>Morning transfer to Varkala. Explore cliffside cafes, Janardhana Swamy Temple, and relax at Papanasam Beach. Sunset view from the cliffs.</p>
                        </div>
                    </div>

                    <div class="day">
                        <div class="day-header">
                            <h4>Day 3 — Ayurveda & Departure</h4>
                            <div class="toggle">+</div>
                        </div>
                        <div class="day-body">
                            <p>Optional Ayurveda therapy session. Breakfast at resort, then checkout and transfer for onward journey.</p>
                        </div>
                    </div>
                </div>

                <div class="price-card">
                    <div class="price-row">
                        <div>Base Price (per person)</div>
                        <div class="price-total">₹13,500</div>
                    </div>
                    <div class="price-row">
                        <div>Taxes & Fees (10%)</div>
                        <div id="taxDisplay">₹1,350</div>
                    </div>
                    <div class="price-row">
                        <div>Selected Guests</div>
                        <div id="priceSummary">1 person</div>
                    </div>
                    <hr style="margin:10px 0; border:none; border-top:1px solid #e6e9ef;" />
                    <div style="font-weight:700; display:flex; justify-content:space-between;">
                        <div>Total Payable</div>
                        <div class="price-total" id="totalPayable">₹14,850</div>
                    </div>
                </div>
            </div>

            <div class="right-col">
                <aside class="booking-box">
                    <h3>Book This Package</h3>
                    <form id="bookingForm" method="post" action="/Home/PaymentPage">
                        <label for="travelDate">Travel Date</label>
                        <input id="travelDate" name="TravelDate" type="date" required />

                        <label for="guests">Guests</label>
                        <select id="guests" name="Guests" required>
                            <option value="1" selected>1 person</option>
                            <option value="2">2 persons</option>
                            <option value="3">3 persons</option>
                            <option value="4">4 persons</option>
                        </select>

                        <label for="contactEmail">Contact Email</label>
                        <input id="contactEmail" name="Email" type="email" placeholder="you@example.com" required />

                        <label for="phone">Phone</label>
                        <input id="phone" name="Phone" type="tel" placeholder="+91 98765 43210" required />

                        <button type="submit" class="btn btn-primary" onclick="saveBeachData()">Book Now</button>
                    </form>
                </aside>
            </div>
        </div>
    </div>

    <script>
        document.addEventListener("DOMContentLoaded", function () {
            // 1. Gallery Logic
            const mainImage = document.getElementById('mainImage');
            const thumbnails = document.querySelectorAll('.thumb');
            thumbnails.forEach(thumb => {
                thumb.addEventListener('click', function () {
                    mainImage.style.backgroundImage = `url('${this.getAttribute('data-src')}')`;
                    thumbnails.forEach(t => t.classList.remove('active'));
                    this.classList.add('active');
                });
            });

            // 2. Accordion Logic
            const dayHeaders = document.querySelectorAll('.day-header');
            dayHeaders.forEach(header => {
                header.addEventListener('click', function () {
                    const body = this.nextElementSibling;
                    const toggleSign = this.querySelector('.toggle');
                    const isOpen = body.style.display === "block";

                    document.querySelectorAll('.day-body').forEach(b => b.style.display = "none");
                    document.querySelectorAll('.toggle').forEach(s => s.textContent = "+");

                    if (!isOpen) {
                        body.style.display = "block";
                        toggleSign.textContent = "-";
                    }
                });
            });

            // 3. Price Calculation
            const guestSelect = document.getElementById('guests');
            const basePrice = 13500;
            const taxRate = 1350;

            guestSelect.addEventListener('change', function() {
                const count = parseInt(this.value);
                const total = (basePrice + taxRate) * count;
                const taxTotal = taxRate * count;

                document.getElementById('priceSummary').textContent = `${count} ${count > 1 ? 'persons' : 'person'}`;
                document.getElementById('taxDisplay').textContent = `₹${taxTotal.toLocaleString('en-IN')}`;
                document.getElementById('totalPayable').textContent = `₹${total.toLocaleString('en-IN')}`;
            });

            // 4. Set Min Date to Today
            const today = new Date().toISOString().split('T')[0];
            document.getElementById('travelDate').setAttribute('min', today);
        });

        // 5. Data Persistence for Payment Gateway
        function saveBeachData() {
            const name = document.getElementById('packageName').innerText;
            const price = document.getElementById('totalPayable').innerText;
            localStorage.setItem('selectedPackageName', name);
            localStorage.setItem('selectedPackagePrice', price);
        }
    </script>
</body>
</html>