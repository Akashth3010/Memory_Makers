@model IEnumerable<TravelPackageManagementSystem.Repository.Models.Destination>
@{
    ViewData["Title"] = "Home Page";
}

<style>
    /* 1. HERO SECTION (MUST BE AT THE TOP) */
    .hero-main {
        min-height: 70vh;
        position: relative;
        display: grid;
        place-items: center;
        text-align: center;
        background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url("https://img.freepik.com/premium-photo/sacred-ganges-river-varanasi-with-ancient-ghats_996201-69539.jpg") center/cover no-repeat;
        color: white;
        width: 100vw;
        left: 50%;
        right: 50%;
        margin-left: -50vw;
        margin-right: -50vw;
    }

    .btn-glow {
        background: linear-gradient(135deg, #ff7b54, #ffd56b);
        border: none;
        padding: 1rem 3rem;
        border-radius: 50px;
        font-weight: 700;
        color: #111;
        text-decoration: none;
        transition: 0.3s;
    }
</style>
<div>

    <partial name="Hero" />

</div>
@* STEP 1: HERO AT TOP *@
@* <header class="hero-main">
    <div class="container">
        <h1 class="display-2 fw-bold">Explore the World</h1>
        <p class="lead mb-4">Handpicked curated travel packages just for you.</p>
        <a href="#destination-section" class="btn btn-glow">Get Inspired</a>
    </div>
</header> *@

@* STEP 2: TOP DESTINATIONS *@
<div id="destination-section" class="py-5">
    @* <partial name="Destination" model="Model" /> *@
    <partial name="Destination" model="Model ?? Enumerable.Empty<TravelPackageManagementSystem.Repository.Models.Destination>()" />
</div>
@* Views/Home/Index.cshtml *@

@* STEP 3: TRENDING SECTION (FIXED GRID) *@
<div id="trending-section" class="py-5">
    <partial name="Trending" model='null' />
</div>
<header class="hero-main">
    <div class="container">
        <h1 class="display-2 fw-bold">Explore the World</h1>
        <p class="lead mb-4">Handpicked curated travel packages just for you.</p>
        <a href="#destination-section" class="btn btn-glow">Get Inspired</a>
    </div>
</header>

@* <partial name="_Footer" />  *@
